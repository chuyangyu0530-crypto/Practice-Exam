// ============================================================================ //
// Implement HTTP Tests for the '/beach/decide' route for 100% statement coverage.
// ============================================================================ //

import request from 'sync-request-curl';
import { port, url } from './config.json';

const SERVER_URL = `${url}:${port}`;

describe('shouldIGoToTheBeach tests', () => {
    // TODO: Implement at most four tests for 100% statement coverage score
});
