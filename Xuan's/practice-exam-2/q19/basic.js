// BASIC JAVASCRIPT QUESTION 7 (10 marks)
// Suggested weighting: 2 + 3 + 2 + 3 marks
// DO NOT MODIFY THE FUNCTION DEFINITIONS

/**
 * Returns the index of the final occurrence of a value in an array.
 * Compare values using strict equality. Return -1 when the value is absent.
 *
 * @param {any[]} arr - Array to search.
 * @param {any} value - Value to locate.
 * @returns {number} - Index of the final matching element, or -1.
 *
 * @example
 * findLastIndex([4, 2, 4, 3], 4) // => 2
 */
export function findLastIndex(arr, value) {
  // TODO: complete me
  return -1;
}

/**
 * Transposes a rectangular two-dimensional array so that rows become columns.
 * The input matrix must not be modified. Return an empty array for an empty
 * matrix. Assume every row has the same length.
 *
 * @param {any[][]} matrix - Rectangular matrix to transpose.
 * @returns {any[][]} - New transposed matrix.
 *
 * @example
 * transpose([[1, 2, 3], [4, 5, 6]]) // => [[1, 4], [2, 5], [3, 6]]
 */
export function transpose(matrix) {
  // TODO: complete me
  return [];
}

/**
 * Creates an object from an array of key-value pairs.
 * Each pair contains a property name followed by its value. When a key occurs
 * more than once, the value from the final pair should be used.
 *
 * @param {Array<[string, any]>} pairs - Key-value pairs.
 * @returns {Object} - Object created from the pairs.
 *
 * @example
 * fromPairs([['name', 'Ari'], ['score', 8]]) // => { name: 'Ari', score: 8 }
 */
export function fromPairs(pairs) {
  // TODO: complete me
  return {};
}

/**
 * Checks whether a phrase is a palindrome.
 * Ignore letter case and all characters that are not English letters or
 * digits. An empty normalized string should be considered a palindrome.
 *
 * @param {string} str - Phrase to check.
 * @returns {boolean} - Whether the normalized phrase reads the same backwards.
 *
 * @example
 * isPalindromePhrase('A man, a plan, a canal: Panama!') // => true
 */
export function isPalindromePhrase(str) {
  // TODO: complete me
  return false;
}
